# Backend tests

This folder is reserved for backend test files.

Suggested convention:
- `*.test.ts` for unit/integration tests
- keep route tests grouped by feature (for example `routes/upload.test.ts`)
